env: dev
kafka:
  bootstrap_servers: "localhost:9092"
  topic: "test-topic-dev"

database:
  warehouse_type: "postgres"
  host: "localhost"
  port: 5432
  database: "dw_dev"
  user: "${DB_USER}"
  password: "${DB_PASS}"